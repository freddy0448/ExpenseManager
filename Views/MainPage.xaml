<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ExpenseManager.ViewModels"
             xmlns:views="clr-namespace:ExpenseManager.Views"
             xmlns:model="clr-namespace:ExpenseManager.Models"
             x:DataType="viewmodels:MainViewModel"
             x:Class="ExpenseManager.MainPage">
    <Shell.BackButtonBehavior>
        <BackButtonBehavior  IsEnabled="False" IsVisible="False"/>
    </Shell.BackButtonBehavior>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Edit" />
    </ContentPage.ToolbarItems>

    <VerticalStackLayout>
        <StackLayout>
            <Label Text="Necesidades del mes" HorizontalOptions="Center" Margin="0,100"/>
        </StackLayout>
    </VerticalStackLayout>
</ContentPage>
