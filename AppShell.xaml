<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ExpenseManager.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ExpenseManager"
    Shell.FlyoutBehavior="Flyout">

    <ShellContent
        Title="Inicio"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage"/>

    <ShellContent Title="Nuevo Gasto"
                  ContentTemplate="{DataTemplate local:Views.AddExpensePage}"/>

    <ShellContent Title="Gastos Sugeridos"
                  ContentTemplate="{DataTemplate local:Views.SuggestedExpensePage}"/>

    <ShellContent Title="Registro"
                  ContentTemplate="{DataTemplate local:Views.SingUpPage}"/>
    
    <ShellContent Title="Inicio de Sesión"
                  ContentTemplate="{DataTemplate local:Views.LogInPage}"/>
</Shell>
